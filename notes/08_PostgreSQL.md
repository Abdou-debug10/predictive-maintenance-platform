Why PostgreSQL?

Password: postgres123
Port: 5432

Current API:
User → Prediction → Response

Problem:
Prediction disappears after response.

Solution:
Store predictions in PostgreSQL.

Benefits:
- Historical prediction tracking
- Dashboard analytics
- Maintenance history
- Future reporting


PostgreSQL Verification

Installation Path:

C:\Program Files\PostgreSQL\17

Command Line Tool:

C:\Program Files\PostgreSQL\17\bin\psql.exe

Purpose:

The psql command-line tool allows direct interaction with PostgreSQL databases.

Verification:

Confirmed psql.exe exists in PostgreSQL installation directory.

PostgreSQL Login

Command:

& "C:\Program Files\PostgreSQL\17\bin\psql.exe" -U postgres

Purpose:

Connect to PostgreSQL server using the default postgres administrator account.

Prompt:

postgres=#

Meaning:

Successfully connected to PostgreSQL.

Database Creation

Database Name:

predictive_maintenance

Command:

CREATE DATABASE predictive_maintenance;

Purpose:

Store prediction history, machine records, maintenance logs, and future analytics data.

Result:

Database successfully created.


Database Status

Database:
predictive_maintenance

Table:
predictions

Schema:

id SERIAL PRIMARY KEY
prediction VARCHAR(100)
confidence FLOAT
created_at TIMESTAMP

Verification:

\dt

Result:

predictions table successfully created.

Purpose:

Store prediction results generated by the machine learning API.


Database Connection Test

File:

api/database.py

Purpose:

Verify that Python can connect to PostgreSQL.

Connection Details:

Host: localhost
Port: 5432
Database: predictive_maintenance
User: postgres

Expected Result:

Database Connected Successfully!

Importance:

Database connectivity must be verified before integrating PostgreSQL with FastAPI.

Database Connection Status

File:

api/database.py

Result:

Database Connected Successfully!

Verified Components:

✓ PostgreSQL Server
✓ Database: predictive_maintenance
✓ Table: predictions
✓ Python Database Connectivity
✓ psycopg2 Driver

Conclusion:

The application can now read and write data to PostgreSQL.


Database Service

File:
api/services/db_service.py

Function:
save_prediction()

Purpose:

Insert prediction records into PostgreSQL.

Stored Fields:

- prediction
- confidence
- created_at

Workflow:

FastAPI
    ↓
save_prediction()
    ↓
PostgreSQL
    ↓
predictions table


Database Insert Test

File:

api/test_db_insert.py

Purpose:

Verify INSERT operation into PostgreSQL.

Inserted Record:

Prediction:
Machine Healthy

Confidence:
99.79

Result:

Record successfully stored in predictions table.

Conclusion:

Database write operations are functioning correctly.




Prediction Storage Verification

Query:

SELECT * FROM predictions;

Result:

id: 1

prediction: Machine Healthy

confidence: 99.79

created_at: Auto Generated Timestamp

Observation:

Prediction records are successfully stored in PostgreSQL and can be retrieved using SQL queries.

Business Value:

Provides prediction history for auditing, analytics, dashboards, and maintenance reporting.